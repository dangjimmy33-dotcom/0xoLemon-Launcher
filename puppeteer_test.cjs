const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => { if (msg.type() === 'error') { console.log('ERROR:', msg.text()); msg.args().forEach(async arg => { try { const props = await arg.getProperties(); for (const [k, v] of props) { if (k === 'stack') console.log('STACK:', await v.jsonValue()); } } catch (e) {} }); } else { console.log('LOG:', msg.text()); } }); await page.goto('http://localhost:1420').catch(()=>{}); await new Promise(r => setTimeout(r, 5000)); await browser.close(); })();
