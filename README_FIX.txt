0xoLemon - Coming Soon tag fix

Files changed:
- src/lib/gameTags.ts
  * Matches tags by game.id, normalized title, and assets/games/<id> path.
- src/components/library.tsx
  * Resolves tags once per card and uses the same result for badge + disabled state.
- src/main.tsx
  * Removes old service-worker registrations and Cache Storage in Tauri, then reloads once if required.
- vite.config.ts
  * Uses vite-plugin-pwa selfDestroying mode to replace the old worker, clear its cache, and unregister it.
- src-tauri/game-tags.json
  * Persona 5 Royal remains configured as: "persona-5-royal": ["coming soon"]

Apply:
1. Close the launcher and stop npm/tauri dev processes.
2. Copy these files over E:\007Launcher using the same paths.
3. Delete E:\007Launcher\dist if it exists.
4. Rebuild:
   cd E:\007Launcher
   npm run tauri:build
5. Run the newly built executable from:
   E:\007Launcher\src-tauri\target\release\0xoLemon.exe
   (or the executable name generated by your Cargo/Tauri configuration.)

The first launch may reload once while removing the old PWA cache. That is expected.
